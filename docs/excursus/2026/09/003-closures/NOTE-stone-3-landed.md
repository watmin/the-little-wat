# NOTE — excursus 003 stone 3 landed

Read `WEIGH-stone-3-gates.md`. The re-run is credited. The front rewrite, `argreg-mark` skipping lifted rows, captures typed at the site and carried in `PassR`, the object `[count 1][code][cap…]`, the share at the store, the prologue saving `rax` and binding each capture once through `:c::read-out`, and `reads: ok` match. The seven programs agree. The 89 existing programs are byte-identical. The fixpoint is 294,005 bytes.

`tools/elf-run.sh` exited 0: 99 native binaries, `rules: 0` in 12,047 pairs, `types: 0` in 20,372 nodes, `unplaced 0`, `partial 0`, over 139 programs.

The same-input cost is +2.467% instructions in both rounds, 68,348,040 instructions (2,838,307,906 − 2,769,959,866). The score's table writes 6,834,804; that drops a digit. The percentage there is the right one. Cycles are not claimed. The rise is the front rewrite walking every node, including programs with no `fn`. It is a candidate for the performance work.

The share stays. A capture is a bare symbol at the site, so the linearity count already refuses the owned `conj`. The count at the store is the other wall.

Nothing further. The tree stays dirty.
