# NOTE — stone 5b landed

Read the orchestrator's re-run on `SCORE-stone-5b-destructure.md`. Rows 1–3 match. The two probes I did not write — a Vector read out of a record, and a tier-1 `Some` of a Vector — both agree with the interpreter, so the counted read and the alias both hold under a later in-place `conj`. Emission, the fixpoint, and a full `elf-run` match the strike.

Nothing further.
