(the Atom (quote fish))
(the Nat 8)
(the (Pair Nat Atom) (cons 3 (quote a)))
(the Nat 5)
(the Nat 0)
(the (-> Nat Nat Nat) (lambda (n j) (iter-Nat n (the Nat j) (lambda (sum-n-1) (add1 sum-n-1)))))
(the (-> Nat Nat) (lambda (j) (add1 (add1 j))))
(the (-> Nat Nat) (lambda (j) (iter-Nat j (the Nat 2) (lambda (sum-n-1) (add1 sum-n-1)))))
(the Atom (quote zero))
(the Nat 2)
(the Atom (quote t))
(the Atom (quote nil))
(the (-> Nat Atom) (lambda (n) (rec-Nat n (the Atom (quote t)) (lambda (n-1 zerop-n-1) (quote nil)))))
(the Nat 0)
(the Nat 15)
(the Nat 55)
(the (-> Nat Nat) (lambda (n) (rec-Nat n (the Nat 0) (lambda (n-1 gauss-n-1) (add1 (iter-Nat n-1 (the Nat gauss-n-1) (lambda (sum-n-1) (add1 sum-n-1))))))))
(the Nat 58)
(the Nat 0)
(the Nat 0)
(the (-> Nat Nat Nat) (lambda (n-1 mul-n-1) (add1 (add1 (add1 mul-n-1)))))
(the (-> Nat Nat) (lambda (j) (iter-Nat j (the Nat (iter-Nat j (the Nat 0) (lambda (sum-n-1) (add1 sum-n-1)))) (lambda (sum-n-1) (add1 sum-n-1)))))
(the (-> Nat Nat) (lambda (j) (rec-Nat j (the Nat 0) (lambda (n-1 mul-n-1) (add1 (add1 mul-n-1))))))
(the Nat 1)
(the Nat 120)
